{
  "name": "mito-website",
  "version": "1.0.0",
  "private": true,
  "scripts": {
    "dev": "npx serve public -p 3000",
    "build": "echo 'Static site — no build step'"
  },
  "devDependencies": {
    "serve": "^14.2.3"
  }
}
